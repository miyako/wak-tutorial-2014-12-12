var PASSWORD_OF_TODAY = 'asdf';function userLogin(userName, password, isKey){		var user = ds.User.find('name == :1', userName);	if(user && password === PASSWORD_OF_TODAY){							return { 	            'ID':generateUUID(), 	            'name':user.name, 	            'fullName':user.name,	            'belongsTo':['User'],	            storage:{'connect':new Date()}	    	};	}else{		return {			'error':1, 			'errorMessage':'User name is invalid.'		}	}}